buildscript {
    repositories {
        gradlePluginPortal()
    }
    dependencies {
        classpath "io.quarkus:gradle-application-plugin:2.7.1.Final"
    }
}

subprojects {
    apply plugin: "java"
    if ("proto-model" != project.name) {
        apply plugin: "io.quarkus"
    }

    sourceCompatibility = 17
    targetCompatibility = 17

    group "ru.craftysoft"
    version "1.0.0"

    ext {
        protobufVersion = "3.19.3"
        quarkusVersion = "2.7.3.Final"
        graphqlVersion = "17.0"
        lombokVersion = "1.18.22"
    }

    dependencies {
        implementation platform("io.quarkus:quarkus-bom:$quarkusVersion")
        if ("proto-model" != project.name) {
            implementation project(":proto-model")
        }
    }

    repositories {
        mavenLocal()
        mavenCentral()
    }

    test {
        useJUnitPlatform()
    }
}

